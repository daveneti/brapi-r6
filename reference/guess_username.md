# Attempts to guess the username from the system environment

Attempts to guess the username from the system environment

## Usage

``` r
guess_username()
```

## Value

The guessed username

## Examples

``` r
if (FALSE) { # \dontrun{
guess_username()
} # }
```
